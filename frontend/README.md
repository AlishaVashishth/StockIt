## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the VITE_API_BASE_URL = https://stockit-production-2ac3.up.railway.app in [.env.local](.env.local)
3. Run the app:
   `npm run dev`
